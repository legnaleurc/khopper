if(MSVC)
    set(LIBRARY_OUTPUT_PATH "${CMAKE_BINARY_DIR}/plugins")
else()
    set(LIBRARY_OUTPUT_PATH "${CMAKE_BINARY_DIR}/lib/plugins")
endif()

add_subdirectory(p_mp3)
add_subdirectory(p_wav)
add_subdirectory(r_flac)
add_subdirectory(xiph)
